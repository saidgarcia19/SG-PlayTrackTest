<Window x:Class="PlayTrackWPF.BooksList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PlayTrackWPF"
        mc:Ignorable="d"
        Title="BooksList" Height="auto" Width="1000">
    <Grid>
        <DataGrid x:Name="bookList" CanUserAddRows="True" AutoGenerateColumns="False" Background="#FF3DA0D9">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Tittle" Binding="{Binding Path=Tittle}" Width="250"/>
                <DataGridTextColumn Header="Author" Binding="{Binding Path=Author}" Width="250"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Path=Category}" Width="250"/>
                <DataGridTextColumn Header="Year" Binding="{Binding Path=Year}" Width="250"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
